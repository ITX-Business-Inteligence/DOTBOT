node_modules/
.env
.env.local
.env.production
*.log
npm-debug.log*
.DS_Store
Thumbs.db
data/*.csv
data/*.xlsx
# Carpetas de uploads y caches — contienen archivos subidos por usuarios
# (Excel imports) y caches del eCFR fetcher.
data/imports/
data/cfrs/cfr-raw/
reports/*.xlsx
reports/*.pdf
.vscode/
.idea/
*.swp
.cache/
build/
coverage/
# dist/docs/ contiene PDFs build artifact. Se generan con
# `node scripts/build-pdfs.js`. Si los queres en el repo (ej. para
# release), commitealos manualmente con `git add -f dist/docs/*.pdf`.
dist/

# Harness / IDE locales
.claude/
.cursor/

# Permitir explicitamente la base de conocimiento CFR (NO ignorar)
!data/cfrs/
!data/cfrs/*.json

# Cloudflared tunnel binary (65MB, no commitear)
cloudflared.exe
