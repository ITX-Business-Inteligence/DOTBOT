@inherits LayoutComponentBase

@* Layout default — solo body content. Cada pagina decide si renderiza
   header, footer, navigation propia. Esto matchea el patron del Node:
   las HTML estaticas no comparten un layout, cada una se autocontiene
   con su propio header. *@
@Body
